# This is the dependabot configuration file that automates dependency updates
# Updates section configures how dependabot should handle dependency updates:
# - Monitors pip dependencies in the planventure-account directory
# - Checks for updates weekly
#
version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/planventure-account"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "python"
